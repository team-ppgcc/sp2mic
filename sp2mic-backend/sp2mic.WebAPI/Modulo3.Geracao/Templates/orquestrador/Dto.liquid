package {{nomePacote}}.dto;


{%- for imp in imports -%}
import {{imp -}};
{%- endfor -%}

import lombok.AllArgsConstructor;
import lombok.Builder;
import lombok.Data;
import lombok.NoArgsConstructor;

@AllArgsConstructor
@NoArgsConstructor
@Data
@Builder
public class {{nomeDtoClasse}} {

{%- for a in atributos -%}
    private {{a.TipoDadoNome}} {{a.NomeAtributo}};
{%- endfor -%}
}
